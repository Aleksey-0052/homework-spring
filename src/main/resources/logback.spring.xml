<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <Properties>
        <property name="LOG_FILE" value="logs/app.log"/>
        <Property name="LOG"
                  value="%-5p %d{yyyy-MM-dd HH:mm:ss, SSS} %c{1.}.%M (%F:%L): %msg%n"/>
        <Property name="LOG_COLOR"
                  value="%highlight{${sys:LOG}}{FATAL=red blink, ERROR=red bold, WARN=yellow, INFO=green,
                  DEBUG=cyan, TRACE=magenta}"/>
    </Properties>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE}.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="com.aston.homework_spring.controller" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </logger>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>

        <!-- ColorConsole: Записывает сообщения в консоль. -->
        <!-- RollingFile: Записывает сообщения в файл logs/app.log и автоматически переключается на новый файл каждый день, -->
        <!-- а также при достижении размера 10MB. Хранит логи за последние 7 дней. -->
        <!-- Мы также определили logger для пакета "com.aston.homework_spring.controller с уровнем DEBUG и указали, что он -->
        <!-- должен использовать оба appender'а. additivity="false" означает, что сообщения, записанные этим logger'ом, -->
        <!-- не будут также записываться root logger'ом. -->
        <!-- Root logger настроен на уровень INFO и также использует оба appender'а.-->
